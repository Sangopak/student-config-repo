spring:
  application:
    name: student-client-local
  profiles:
    active: local
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true  
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: 
    driver-class-name: org.h2.Driver    
student-client-local:
  ribbon:
    listOfServers: http://localhost:8080,http://localhost:8081    